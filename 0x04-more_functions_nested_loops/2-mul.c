#include "main.h"

/**
 *mul - is a function for multiplies two integers.
 *@a: first input.
 *@b: second input.
 *Return: result of multiply the two integers.
 */
int mul(int a, int b)
{
	return (a * b);
}
